# 🤖 TestBot - Marketing Project

> 📝 **Система управления данными**  
> Полноценное веб-приложение для сохранения и управления пользовательскими данными с использованием современного стека технологий.

![React](https://img.shields.io/badge/React-18-blue)
![Node.js](https://img.shields.io/badge/Node.js-Express-green)
![PostgreSQL](https://img.shields.io/badge/Database-PostgreSQL-orange)
![TypeScript](https://img.shields.io/badge/TypeScript-Ready-blue)

## 🛠 Технологии

- **Frontend**: React 18 + TypeScript
- **Backend**: Node.js + Express
- **База данных**: PostgreSQL
- **HTTP клиент**: Axios
- **Стилизация**: CSS3 с современным дизайном

## 📋 Функциональность

- ✅ Форма для заполнения пользовательских данных
- ✅ Сохранение данных в PostgreSQL с уникальным UUID
- ✅ Просмотр всех сохраненных записей
- ✅ Редактирование существующих записей
- ✅ Удаление записей
- ✅ Валидация данных на фронтенде и бэкенде
- ✅ Responsive дизайн
- ✅ Уведомления об успешных операциях и ошибках

## 🚀 Быстрый запуск

### Предварительные требования

1. **Node.js** (версия 16 или выше)
2. **PostgreSQL** (версия 12 или выше)
3. **npm** или **yarn**

### 📦 Установка зависимостей

```bash
# Установка зависимостей для всех частей проекта
npm run install-all

# Или установка по отдельности:
# Корневые зависимости
npm install

# Backend зависимости
cd backend && npm install

# Frontend зависимости
cd frontend && npm install
```

### 🗄️ Настройка базы данных

1. **Запустите PostgreSQL** на вашей системе

2. **Создайте базу данных**:
```sql
CREATE DATABASE marketing_db;
```

3. **Настройте файл конфигурации**:
```bash
# Скопируйте файл конфигурации
cp backend/config.env.example backend/.env

# Или переименуйте существующий файл
mv backend/config.env backend/.env
```

4. **Отредактируйте `backend/.env`**:
```env
# Конфигурация базы данных
DB_HOST=localhost
DB_PORT=5432
DB_NAME=marketing_db
DB_USER=your_username
DB_PASSWORD=your_password

# Конфигурация сервера
PORT=5001
NODE_ENV=development
```

> 📝 **Важно**: Замените `your_username` и `your_password` на ваши реальные данные PostgreSQL

### 🎯 Запуск приложения

#### Вариант 1: Запуск всего проекта одной командой (рекомендуется)

```bash
npm run dev-all
```

Эта команда запустит одновременно:
- Backend сервер на `http://localhost:5001`
- Frontend приложение на `http://localhost:3000`

#### Вариант 2: Запуск по отдельности

**Терминал 1 - Backend:**
```bash
cd backend
npm run dev
```

**Терминал 2 - Frontend:**
```bash
cd frontend
npm start
```

### 🌐 Доступ к приложению

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:5001
- **API документация**: http://localhost:5001 (JSON с описанием эндпоинтов)

## 📁 Структура проекта

```
marketing-project/
├── README.md                 # Документация проекта
├── package.json             # Корневые зависимости и скрипты
├── backend/                 # Backend на Node.js
│   ├── package.json        # Backend зависимости
│   ├── server.js           # Основной сервер Express
│   ├── database.js         # Конфигурация PostgreSQL
│   └── config.env          # Переменные окружения
└── frontend/               # Frontend на React
    ├── package.json        # Frontend зависимости
    ├── public/            # Статические файлы
    └── src/               # Исходный код React
        ├── components/    # React компоненты
        ├── services/      # API сервисы
        ├── types/         # TypeScript типы
        ├── App.tsx        # Главный компонент
        └── App.css        # Стили приложения
```

## 🔧 API Эндпоинты

| Метод | URL | Описание |
|-------|-----|----------|
| `GET` | `/api/data` | Получить все записи |
| `GET` | `/api/data/:id` | Получить запись по ID |
| `POST` | `/api/data` | Создать новую запись |
| `PUT` | `/api/data/:id` | Обновить запись |
| `DELETE` | `/api/data/:id` | Удалить запись |

### Пример данных для POST/PUT запросов:

```json
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "phone": "+7 (900) 123-45-67",
  "message": "Тестовое сообщение"
}
```

## 📊 Схема базы данных

Таблица `user_data`:

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | UUID | Уникальный идентификатор (PRIMARY KEY) |
| `name` | VARCHAR(255) | Имя пользователя (обязательное) |
| `email` | VARCHAR(255) | Email пользователя (обязательное) |
| `phone` | VARCHAR(50) | Номер телефона (опционально) |
| `message` | TEXT | Сообщение (опционально) |
| `created_at` | TIMESTAMP | Дата создания |
| `updated_at` | TIMESTAMP | Дата последнего обновления |

## 🎨 Особенности дизайна

- 🎨 Современный Material Design подход
- 📱 Полностью адаптивный дизайн
- 🌈 Градиентные фоны и красивые тени
- ✨ Плавные анимации и переходы
- 📬 Система уведомлений для пользователя
- 🔄 Индикаторы загрузки

## 🛡️ Безопасность и валидация

- ✅ Валидация данных на фронтенде
- ✅ Серверная валидация на бэкенде
- ✅ Защита от SQL инъекций через параметризованные запросы
- ✅ CORS настройки для безопасности
- ✅ Обработка ошибок с информативными сообщениями

## 🐛 Отладка и решение проблем

### Проблемы с подключением к базе данных:

1. Убедитесь, что PostgreSQL запущен:
```bash
# На macOS с Homebrew
brew services start postgresql

# На Ubuntu/Debian
sudo systemctl start postgresql

# На Windows - запустите через службы
```

2. Проверьте настройки в файле `.env`
3. Убедитесь, что пользователь и база данных существуют

### Проблемы с портами:

- Backend по умолчанию использует порт `5001`
- Frontend по умолчанию использует порт `3000`
- Если порты заняты, измените их в конфигурации

### Логи и отладка:

- Backend логи выводятся в консоль с эмодзи для удобства
- Frontend ошибки отображаются в браузерной консоли
- Проверьте Network вкладку в DevTools для API запросов

## 📈 Развитие проекта

Возможные улучшения:
- 🔐 Добавление аутентификации пользователей
- 📄 Пагинация для больших списков данных
- 🔍 Поиск и фильтрация записей
- 📁 Группировка по категориям
- 📊 Экспорт данных в CSV/Excel
- 🔔 Push уведомления
- 🌍 Мультиязычность

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте этот README
2. Изучите логи в консоли
3. Убедитесь, что все зависимости установлены
4. Проверьте настройки базы данных

---

**Создано с ❤️ на React + Node.js + PostgreSQL**
