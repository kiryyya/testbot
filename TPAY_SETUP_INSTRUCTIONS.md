# Инструкция по настройке T-Pay для тестирования

## ⚠️ Важно: Белый список IP-адресов

Для использования **тестового API T-Pay** (`https://rest-api-test.tinkoff.ru/v2/Init`) необходимо добавить ваш IP-адрес в белый список.

### Шаг 1: Получение вашего IP-адреса

Выполните одну из команд:

```bash
# Linux/Mac
curl ifconfig.me

# Или
curl ipinfo.io/ip

# Или через браузер
# Откройте https://ifconfig.me
```

### Шаг 2: Отправка запроса на добавление в белый список

Отправьте email на **acq_help@tbank.ru** с следующей информацией:

**Тема:** Запрос на добавление IP-адреса в белый список тестовой среды

**Содержание:**
```
Здравствуйте!

Прошу добавить мой IP-адрес в белый список тестовой среды T-Pay.

Данные:
- ИНН организации: [ваш ИНН]
- Наименование организации: [название]
- IP-адрес: [ваш IP-адрес]
- Тестовый URL: https://rest-api-test.tinkoff.ru/v2/Init

Спасибо!
```

### Шаг 3: Настройка переменных окружения

После получения доступа, добавьте в `backend/config.env`:

```env
# Режим тестирования
TPAY_TEST_MODE=true

# Тестовые ключи (получите в личном кабинете T-Pay)
TPAY_TERMINAL_KEY=your_test_terminal_key
TPAY_PASSWORD=your_test_password

# Опционально: Bearer токен для авторизации
TPAY_API_TOKEN=your_test_api_token

# URL для callback (должен быть доступен извне)
TPAY_NOTIFICATION_URL=https://yourdomain.com/api/payments/tpay-callback

# URL для возврата пользователя
TPAY_RETURN_URL=https://yourdomain.com/payment/success
```

### Шаг 4: Проверка подключения

1. Запустите сервер:
   ```bash
   cd backend
   npm start
   ```

2. Попробуйте создать платеж через интерфейс

3. Проверьте логи сервера - должны быть видны:
   - `📤 Отправка запроса в T-Pay (ТЕСТ)`
   - `✅ Ответ от T-Pay` или `❌ Ошибка вызова T-Pay API`

## Альтернатива: Использование мок-данных

Если нет доступа к тестовому API, система автоматически использует мок-данные:
- Не указывайте `TPAY_TERMINAL_KEY` и `TPAY_PASSWORD`
- Система вернет мок `paymentUrl` для тестирования UI

## Проблемы и решения

### Ошибка: "Connection refused" или "ECONNREFUSED"
- **Причина:** IP-адрес не добавлен в белый список
- **Решение:** Отправьте запрос на acq_help@tbank.ru

### Ошибка: "Invalid Token" или "Неверная подпись"
- **Причина:** Неправильно сформирована подпись
- **Решение:** Проверьте, что `TPAY_PASSWORD` указан правильно

### Ошибка: "TerminalKey not found"
- **Причина:** Неверный TerminalKey
- **Решение:** Проверьте TerminalKey в личном кабинете T-Pay

### Ошибка: "Amount must be positive"
- **Причина:** Сумма должна быть в копейках и больше 0
- **Решение:** Система автоматически конвертирует рубли в копейки

## Полезные ссылки

- Документация T-Pay: https://developer.tbank.ru/eacq/api/init
- Личный кабинет: https://business.tinkoff.ru
- Поддержка: acq_help@tbank.ru

