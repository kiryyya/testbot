# Игровая механика с рандомным уроном

## Обзор игровой системы

Каждый пользователь, который оставляет комментарий в сообществе ВКонтакте, автоматически становится игроком со следующими характеристиками:

### Начальные параметры игрока:
- 🎮 **Попытки**: 5
- 💜 **Жизни**: 100  
- ⭐ **Очки**: 0

## Механика игры

### При каждом комментарии:
1. **Проверка попыток**: Если попытки = 0, игра завершена
2. **Попытка тратится**: -1 попытка (если попытки > 0)
3. **Случайный урон жизней**: от 1 до 25 жизней (Math.random)
4. **Очки зарабатываются**: +1 очко за комментарий

### Формула случайного урона:
```javascript
const damage = Math.floor(Math.random() * (25 - 1 + 1)) + 1;
// Результат: число от 1 до 25 включительно
```

## Условия победы

Игрок **ПОБЕЖДАЕТ**, когда выполняются **ОБЕ** условия:
1. ✅ Использованы все 5 попыток (`attempts_left <= 0`)
2. ✅ Потрачены все жизни (`lives_count <= 0`)

## Примеры игрового процесса

### Пример 1: Обычный ход
**Комментарий**: "Привет!"
**Случайный урон**: 15 жизней

**Ответ бота**:
```
привет удачно
🎮 Попытки: 4 | 💜 Жизни: 85 | ⭐ Очки: 1
💥 Урон: -15 жизней
```

### Пример 2: Критическое состояние
**Комментарий**: "Как дела?"
**Случайный урон**: 22 жизни
**Состояние**: 1 попытка, 18 жизней

**Ответ бота**:
```
как дела удачно
🎮 Попытки: 1 | 💜 Жизни: 18 | ⭐ Очки: 4
💥 Урон: -22 жизней
⚠️ Осталась последняя попытка!
💔 Жизней мало!
```

### Пример 3: Попытки закончились
**Комментарий**: "Еще один комментарий"
**Состояние**: 0 попыток, 25 жизней

**Ответ бота**:
```
еще один комментарий удачно

🚫 ПОПЫТКИ ЗАКОНЧИЛИСЬ! 🚫

У вас больше нет попыток для игры.

📊 Ваша статистика:
🎮 Попытки: 0 | 💜 Жизни: 25 | ⭐ Очки: 5

💡 Жизни еще остались, но попытки кончились. Игра завершена.
```

### Пример 4: ПОБЕДА!
**Комментарий**: "Финал"
**Случайный урон**: 18 жизней (добивает остатки)
**Результат**: 0 попыток, 0 жизней

**Ответ бота**:
```
финал удачно

🎉🏆 ВЫ ПОБЕДИЛИ! 🏆🎉

Вы прошли все 5 попыток и потратили все жизни! Поздравляем с победой! 🎊

📊 Финальная статистика:
⭐ Итоговые очки: 5
💀 Последний урон: -18 жизней
```

## Структура базы данных

### Таблица `vk_events` хранит:
- `attempts_used` - сколько попыток потрачено (всегда 1)
- `lives_used` - случайный урон жизней (1-25)
- `score_earned` - заработанные очки (всегда 1)

### Функции расчета:

#### `calculateRandomDamage()`
```javascript
const calculateRandomDamage = () => {
  const minDamage = 1;
  const maxDamage = 25;
  return Math.floor(Math.random() * (maxDamage - minDamage + 1)) + minDamage;
};
```

#### `checkVictoryConditions(player)`
```javascript
const checkVictoryConditions = (player) => {
  const hasUsedAllAttempts = player.attempts_left <= 0;
  const hasLost100Lives = player.lives_count <= 0;
  return hasUsedAllAttempts && hasLost100Lives;
};
```

## Логика обработки комментария

1. **Найти/создать игрока** → `findOrCreateVkPlayer()`
2. **Рассчитать урон** → `calculateRandomDamage()`
3. **Создать событие** → `createVkEvent()` 
4. **Обновить статистику** → `updatePlayerStats()`
5. **Проверить победу** → `checkVictoryConditions()`
6. **Отправить ответ** → `replyToComment()`

## Сообщения в зависимости от состояния

### Предупреждения:
- `attempts_left <= 1`: "⚠️ Осталась последняя попытка!"
- `attempts_left <= 2`: "🔥 Попыток мало, будь осторожнее!"
- `lives_count <= 20`: "💔 Жизней мало!"
- `lives_count <= 0`: "💀 Жизни закончились!"

### Победа:
```
🎉🏆 ВЫ ПОБЕДИЛИ! 🏆🎉
Вы прошли все 5 попыток и потратили все жизни! Поздравляем с победой! 🎊
```

## Вероятности урона

При равномерном распределении от 1 до 25:
- Средний урон: **13 жизней**
- Минимальные 100 жизней тратятся за: **4-100 комментариев**
- Математическое ожидание: **~7.7 комментариев** для трат 100 жизней

Таким образом, в среднем игрок победит за 5 попыток, потратив примерно 65 жизней (5 × 13), но возможны как быстрые победы (при больших уронах), так и "выживание" (при маленьких уронах).
