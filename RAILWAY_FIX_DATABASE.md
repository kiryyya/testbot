# 🔧 Исправление: Подключение PostgreSQL к сервису

## Проблема
PostgreSQL база данных создана, но не связана с сервисом "app", поэтому переменная `DATABASE_URL` недоступна.

## Решение (через веб-интерфейс Railway)

### Шаг 1: Откройте ваш проект
https://railway.com/project/9c893e2a-cf9a-4db2-8434-4ba376b03a38

### Шаг 2: Найдите PostgreSQL сервис
В проекте вы увидите несколько сервисов:
- **app** (ваш backend)
- **Postgres** (база данных)

### Шаг 3: Свяжите PostgreSQL с app сервисом

**Вариант А: Через Variables**
1. Кликните на сервис **"app"**
2. Перейдите в **"Variables"**
3. Нажмите **"+ New Variable"**
4. Выберите **"Add Reference"**
5. В выпадающем списке найдите **"Postgres"**
6. Выберите переменную **"DATABASE_URL"**
7. Нажмите **"Add"**

**Вариант Б: Автоматическая связь (рекомендуется)**
1. Кликните на **PostgreSQL** сервис
2. Перейдите в **"Connect"**
3. Найдите секцию **"Available connections"** или **"Link to service"**
4. Выберите сервис **"app"**
5. Railway автоматически добавит `DATABASE_URL` в переменные сервиса "app"

### Шаг 4: Redeploy backend
После добавления DATABASE_URL:
1. Перейдите в сервис **"app"**
2. Вкладка **"Deployments"**
3. Нажмите три точки (⋮) на последнем деплое
4. Выберите **"Redeploy"**

---

## После исправления

Backend должен успешно запуститься и подключиться к базе данных! 🎉

Проверьте:
```
curl https://app-production-93a2.up.railway.app
```

Должен вернуть JSON с информацией об API.

