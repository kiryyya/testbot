# 🚂 Гайд по деплою на Railway

## 📋 Содержание
1. [Подготовка проекта](#подготовка-проекта)
2. [Регистрация на Railway](#регистрация-на-railway)
3. [Деплой Backend](#деплой-backend)
4. [Деплой Frontend](#деплой-frontend)
5. [Настройка VK приложения](#настройка-vk-приложения)
6. [Проверка работоспособности](#проверка-работоспособности)
7. [Troubleshooting](#troubleshooting)

---

## 🎯 Подготовка проекта

### ✅ Что уже готово:
- ✅ `.gitignore` настроен для защиты секретов
- ✅ `.env.example` файлы созданы
- ✅ CORS настроен для production
- ✅ Railway конфигурация создана
- ✅ Package.json scripts готовы

### ⚠️ Перед началом убедитесь:
- [ ] У вас есть аккаунт VK и созданное VK приложение
- [ ] У вас есть OpenAI API ключ (если используете GPT автоответы)
- [ ] Проект закоммичен в Git репозиторий (GitHub/GitLab)

---

## 🚀 Регистрация на Railway

### Шаг 1: Создайте аккаунт
1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"**
3. Авторизуйтесь через GitHub

### Шаг 2: Подключите GitHub репозиторий
1. Загрузите ваш проект на GitHub (если еще не загружен):
   ```bash
   git add .
   git commit -m "Prepare for Railway deployment"
   git push origin main
   ```

---

## 🔧 Деплой Backend

### Шаг 1: Создайте новый проект
1. В Railway Dashboard нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий `testbot`

### Шаг 2: Добавьте PostgreSQL базу данных
1. В проекте нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
2. Railway автоматически создаст базу данных
3. Railway автоматически добавит переменную `DATABASE_URL` в backend

### Шаг 3: Настройте Backend сервис
1. Нажмите на ваш backend сервис
2. Перейдите в **"Settings"**
3. В разделе **"Root Directory"** укажите: `backend`
4. В разделе **"Start Command"** должно быть: `npm start` (уже настроено)

### Шаг 4: Добавьте Environment Variables
Перейдите в **"Variables"** и добавьте:

```env
# Database (Railway предоставит автоматически DATABASE_URL)
# Но если нужны отдельные параметры:
DB_HOST=${{Postgres.PGHOST}}
DB_PORT=${{Postgres.PGPORT}}
DB_NAME=${{Postgres.PGDATABASE}}
DB_USER=${{Postgres.PGUSER}}
DB_PASSWORD=${{Postgres.PGPASSWORD}}

# Server
NODE_ENV=production

# VK API
VK_APP_ID=54125757
VK_APP_SECRET=ваш_секретный_ключ_из_vk

# VK OAuth (ВАЖНО: обновите после деплоя frontend!)
VK_REDIRECT_URI=https://your-frontend-app.up.railway.app/auth/vk/callback

# Callback API (ВАЖНО: обновите после получения backend URL!)
CALLBACK_URL=https://your-backend-app.up.railway.app/vk/callback

# OpenAI
OPENAI_API_KEY=ваш_openai_api_ключ

# Frontend URL для CORS (ВАЖНО: обновите после деплоя frontend!)
FRONTEND_URL=https://your-frontend-app.up.railway.app
```

### Шаг 5: Получите Backend URL
1. После деплоя перейдите в **"Settings"** → **"Networking"**
2. Нажмите **"Generate Domain"**
3. Скопируйте ваш backend URL (например: `https://testbot-backend.up.railway.app`)
4. **ВАЖНО:** Обновите переменную `CALLBACK_URL` с этим URL

### Шаг 6: Проверьте деплой
1. Откройте в браузере: `https://your-backend-app.up.railway.app`
2. Вы должны увидеть JSON с описанием API

---

## 🎨 Деплой Frontend

### Шаг 1: Добавьте Frontend в проект
1. В том же Railway проекте нажмите **"+ New"** → **"GitHub Repo"**
2. Выберите тот же репозиторий `testbot`

### Шаг 2: Настройте Frontend сервис
1. Нажмите на новый сервис
2. Перейдите в **"Settings"**
3. В разделе **"Root Directory"** укажите: `frontend`
4. В разделе **"Build Command"** должно быть: `npm run build`
5. В разделе **"Start Command"** укажите: `npx serve -s build -l $PORT`

### Шаг 3: Добавьте Environment Variables
Перейдите в **"Variables"** и добавьте:

```env
# Backend API URL (замените на ваш backend URL из предыдущего шага!)
REACT_APP_API_URL=https://your-backend-app.up.railway.app/api

# VK App ID
REACT_APP_VK_APP_ID=54125757
```

### Шаг 4: Установите serve (для статического хостинга)
Добавьте в `frontend/package.json` в секцию `dependencies`:
```json
"serve": "^14.2.0"
```

Или Railway автоматически установит при деплое.

### Шаг 5: Получите Frontend URL
1. После деплоя перейдите в **"Settings"** → **"Networking"**
2. Нажмите **"Generate Domain"**
3. Скопируйте ваш frontend URL (например: `https://testbot.up.railway.app`)

### Шаг 6: Обновите Backend переменные
Вернитесь в Backend сервис и обновите:
```env
FRONTEND_URL=https://your-actual-frontend-url.up.railway.app
VK_REDIRECT_URI=https://your-actual-frontend-url.up.railway.app/auth/vk/callback
```

### Шаг 7: Пересоберите сервисы
1. В Backend: **"Deployments"** → **"Redeploy"**
2. В Frontend: **"Deployments"** → **"Redeploy"**

---

## 🔐 Настройка VK приложения

### Шаг 1: Обновите Redirect URI
1. Перейдите в [VK Apps Manager](https://vk.com/apps?act=manage)
2. Выберите ваше приложение (ID: 54125757)
3. Перейдите в **"Настройки"**
4. В разделе **"Authorized redirect URIs"** добавьте:
   ```
   https://your-frontend-app.up.railway.app/auth/vk/callback
   ```
5. Сохраните изменения

### Шаг 2: Настройте Callback API в VK
1. Откройте ваше сообщество ВКонтакте
2. Перейдите в **"Управление"** → **"Работа с API"** → **"Callback API"**
3. Нажмите **"Добавить сервер"**
4. Укажите:
   - **URL:** `https://your-backend-app.up.railway.app/vk/callback`
   - **Подтверждение:** получите из БД или через ваше приложение
   - **Секретный ключ:** создайте уникальный ключ
5. Включите типы событий:
   - ✅ Новое сообщение
   - ✅ Новый комментарий к записи
   - ✅ Добавление лайка
   - ✅ Удаление лайка

**💡 Совет:** Ваше приложение автоматически настроит Callback API при добавлении сообщества через OAuth!

---

## ✅ Проверка работоспособности

### 1. Проверьте Backend
```bash
curl https://your-backend-app.up.railway.app
```
Должен вернуть JSON с информацией об API.

### 2. Проверьте База данных
В Railway Dashboard → PostgreSQL → **"Connect"** → скопируйте команду и выполните:
```bash
psql $DATABASE_URL
```

Проверьте таблицы:
```sql
\dt
```

### 3. Проверьте Frontend
Откройте в браузере: `https://your-frontend-app.up.railway.app`

### 4. Проверьте VK интеграцию
1. Войдите в ваше приложение
2. Попробуйте добавить сообщество
3. Отправьте тестовый комментарий в VK
4. Проверьте, что бот ответил

---

## 🐛 Troubleshooting

### Проблема: Backend не стартует

**Решение:**
1. Проверьте логи в Railway: **"Deployments"** → выберите деплой → **"View Logs"**
2. Убедитесь, что все environment variables заданы
3. Проверьте, что `DATABASE_URL` присутствует

### Проблема: Frontend показывает ошибку CORS

**Решение:**
1. Убедитесь, что `FRONTEND_URL` в backend установлен правильно
2. Проверьте, что в frontend `REACT_APP_API_URL` указывает на правильный backend URL
3. Пересоберите оба сервиса

### Проблема: База данных не подключается

**Решение:**
1. Проверьте в Railway: PostgreSQL сервис должен быть **"Active"**
2. Убедитесь, что backend и база данных в одном проекте
3. Railway автоматически связывает сервисы

### Проблема: VK OAuth не работает

**Решение:**
1. Проверьте, что `VK_REDIRECT_URI` совпадает с настройками в VK Apps
2. Убедитесь, что в VK Apps добавлен production URL
3. Проверьте логи backend на ошибки OAuth

### Проблема: VK Callback не работает

**Решение:**
1. Проверьте, что `CALLBACK_URL` правильный
2. В VK Callback API должен быть установлен правильный URL сервера
3. Проверьте confirmation code в настройках сообщества
4. Убедитесь, что секретный ключ совпадает

### Проблема: OpenAI не отвечает

**Решение:**
1. Проверьте, что `OPENAI_API_KEY` установлен
2. Проверьте баланс на [OpenAI Dashboard](https://platform.openai.com/usage)
3. В логах backend должны быть ошибки от OpenAI API

---

## 📊 Мониторинг

### Railway Dashboard
- **Metrics:** CPU, Memory, Network использование
- **Logs:** Все логи в реальном времени
- **Deployments:** История деплоев

### Полезные команды

Просмотр логов:
```bash
railway logs
```

Запуск локальной среды с Railway переменными:
```bash
railway run npm start
```

---

## 💰 Стоимость

**Railway Free Tier включает:**
- $5 бесплатных credits в месяц
- До 500 часов работы сервисов
- 1 GB RAM на сервис
- PostgreSQL база данных

**Примерная стоимость после Free Tier:**
- ~$5-10 в месяц для малого проекта
- Оплата за реальное использование (CPU, RAM, Network)

---

## 🎉 Готово!

Ваш проект теперь работает на Railway!

**Финальный чеклист:**
- [ ] Backend работает и отвечает на запросы
- [ ] Frontend открывается в браузере
- [ ] База данных подключена
- [ ] VK OAuth работает
- [ ] VK Callback API настроен
- [ ] Автоответы бота работают
- [ ] Игровая система активна

---

## 📚 Полезные ссылки

- [Railway Documentation](https://docs.railway.app/)
- [Railway CLI](https://docs.railway.app/develop/cli)
- [VK API Documentation](https://dev.vk.com/ru/api)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

**Создано с ❤️ для деплоя на Railway**

